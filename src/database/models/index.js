const { UserModel } = require('./user.model');
const { RoomModel } = require('./room.model');
const { ChatMessageModel } = require('./chat_message.model');
const { WhiteboardModel } = require('./whiteboard.model');

module.exports = {
  UserModel,
  RoomModel,
  ChatMessageModel,
  WhiteboardModel,
};
